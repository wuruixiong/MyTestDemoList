<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wrx.mytest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light.NoActionBar"
        >

        <meta-data
            android:name="com.wrx.mytest.glide.ImageGlideModule"
            android:value="GlideModule" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestActivity"/>
        <activity android:name=".recycle.RecycleTouchHelpActivity"/>
        <activity android:name=".GesturesActivity"/>
        <activity android:name=".glide.GlideActivity"/>
        <activity android:name=".http.OkHttpActivity"/>
        <activity android:name=".clip.PathActivity"/>
        <activity android:name=".recycle.RecycleBtDeleteActivity"/>
        <activity android:name=".recycle.RecycleMeActivity"/>

    </application>

</manifest>