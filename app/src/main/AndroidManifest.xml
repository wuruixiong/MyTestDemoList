<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wrx.mytest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light"
        >

        <!-- for open GL -->
        <uses-feature android:glEsVersion="0x00020000" android:required="true" />
        <supports-gl-texture android:name="GL_OES_compressed_ETC1_RGB8_texture" />
        <supports-gl-texture android:name="GL_OES_compressed_paletted_texture" />
        <!-- for open GL -->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestActivity"
            />
        <activity android:name=".opengl.GLSurActivity"/>
        <activity android:name=".opengl.GLSurActivity2"/>
        <activity android:name=".ViewLifecycleActivity"/>
        <activity android:name=".TouchTestActivity"/>

        <activity android:name=".textIntent.IntentTestActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".textIntent.IntentTestActivity2"
            android:launchMode="singleTask"/>
        <activity android:name=".textIntent.IntentTestActivity3"
            android:launchMode="singleTask"/>

        <activity android:name=".ElevationActivity" />
        <activity android:name=".MultiplePointersActivity" />
        <activity android:name=".MaskActivity" />
        <activity android:name=".TextMaskActivity"
            android:theme="@style/myTransparent"
            />
        <activity android:name=".TextBeMaskActivity"
            />
        <activity android:name=".MixViewEfActivity"
            android:theme="@style/MyBarTheme"
            />
        <activity android:name=".ZoomActivity" />
        <activity android:name=".LoginActivity" android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".InputTypeActivity" />
    </application>

</manifest>